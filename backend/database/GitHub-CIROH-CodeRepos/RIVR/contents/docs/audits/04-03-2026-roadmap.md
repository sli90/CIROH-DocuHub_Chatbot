# RIVR Quick Wins Roadmap
**Audit:** `04-03-2026.md`
**Score at audit:** 459 / 1000 (Grade D)

Items sourced from the Top 10 Gaps — high impact, low effort.

---

## Quick Wins

- [x] Install `firebase_crashlytics` and wire it to the existing `FlutterError.onError` and `PlatformDispatcher.instance.onError` error boundaries — eliminates blind production crashes *(completed 2026-04-03)*
- [x] Version-control `firestore.rules` — add to repo, scope rules to per-user access, deploy via CLI — eliminates a security blind spot *(completed 2026-04-03)*
- [x] Set up GitHub Actions with `flutter test` + `flutter analyze` on every push — eliminates the no-safety-net-on-push problem *(completed 2026-04-03)*
- [x] Configure release obfuscation — add `--obfuscate` and `--split-debug-info` to release build commands so class/method names are not readable in the release binary *(audit §6.4, §5.5) (completed 2026-04-03)*
- [x] Instrument Firebase Analytics — `firebase_analytics` is installed but has zero `logEvent()` calls; add events for screen views, forecast loads, favorites added/removed, alert toggles *(audit §10.2, §14.9) (completed 2026-04-03)*
- [x] Fix `FCMService.onTokenRefresh` subscription leak — store the `StreamSubscription` reference and cancel it on teardown; currently the listener can never be cancelled *(audit §14.6) (completed 2026-04-03)*

---

## Medium Effort

- [x] Add shimmer/skeleton loading screens — replace all `CupertinoActivityIndicator` spinners with animated placeholder cards on the favorites list and forecast overview; most visible UX gap *(audit §9.1) (completed 2026-04-03)*
- [x] Add offline / connectivity detection — add `connectivity_plus`, detect when the device goes offline, and show a persistent banner; critical for an outdoor app used near rivers *(audit §9.5) (completed 2026-04-03)*
- [x] Add text scale factor support — sweep for fixed font sizes that overflow at iOS system font ×1.5 and ×2.0; add `TextScaler`-aware layouts on critical screens *(audit §9.6) (completed 2026-04-03)*

---

## Architectural (Long-term)

- [x] Replace `SharedPreferences` with file-based JSON for `ReachData` persistence — one file per reach under `getApplicationCacheDirectory()/rivr_reach_cache/`; better I/O semantics, individual file cleanup, no cross-key collisions *(completed 2026-04-04)*
- [x] Add request cancellation — generation-counter pattern in `ReachDataProvider` and `FavoritesProvider` prevents stale in-flight futures from applying results after navigation *(completed 2026-04-04)*
- [x] Migrate to Clean Architecture — repository interfaces and use case classes added for all 5 features (auth, favorites, forecast, map, settings); domain layer lives in `lib/features/<feature>/domain/` *(completed 2026-04-04)*
